@model HomeViewModel


<h2>The Alcoholic Beverages</h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var item in Model.AlcoholItems)
    {
        @await Html.PartialAsync("UnitItemSummary", item)
    }
</div>