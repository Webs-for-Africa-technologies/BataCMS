<li class="dropdown">
    <a asp-controller="UnitItem"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Items<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="UnitItem" asp-action="List"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="UnitItem" asp-action="List" asp-route-category="">Show all Items</a>
        </li>
    </ul>
</li>